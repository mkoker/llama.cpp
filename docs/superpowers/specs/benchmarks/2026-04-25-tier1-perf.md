# Tier 1 performance gate — 2026-04-28

## Gate command executed
`llama-bench -m /mnt/nvme/models/Qwen3-30B-A3B-Q4_K_M.gguf -ngl 99 -fa 1 -ncmoe 8 --expert-cache-size 8192 -p 0 -n 128 -r 1`

## Result
- Model load failed before benchmark token generation:
  - `main: error: failed to load model /mnt/nvme/models/Qwen3-30B-A3B-Q4_K_M.gguf`
- Root cause from direct llama-cli repro: ROCm OOM at `-ngl 99`
  - `allocating 14635.43 MiB on device 0: cudaMalloc failed: out of memory`

## Gate script bug found
Current awk check returns success when `tg128` line is missing, so the gate can false-pass on model-load failure.

## Additional probe runs (same build)
- `-ngl 10 -ncmoe 8 --expert-cache-size 8192`: `tg128 3.82 t/s`
- `-ngl 10 -ncmoe 0 --expert-cache-size 0`: `tg128 3.40 t/s`

## Conclusion
Task 11 acceptance (`cache-on tg128 >= 82.8`) is not currently achievable on this VM/config because the specified gate setting `-ngl 99` fails model load from VRAM OOM.
